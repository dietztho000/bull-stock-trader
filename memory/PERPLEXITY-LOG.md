# Perplexity Query Log

Append-only log of every Perplexity API call. Daily-summary tallies the
day's calls and reports the count + estimated cost. If calls/day exceeds
2x the rolling 14-day median, daily-summary fires a Discord error — that
means a prompt regression is looping calls inside a single routine.

Pricing (sonar): roughly $0.0005 per query at the typical token shape used
by this bot. Update this header if the model or pricing changes.

## Queries

| Timestamp | Model | Query (truncated to 200 chars) |
|-----------|-------|--------------------------------|
| _populated by scripts/perplexity.sh on every call_ | | |
| 2026-04-30 22:29 UTC | sonar | What are today's key market catalysts and top momentum stock opportunities for April 30, 2026? Include: S&P 500 futures direction, VIX level, WTI oil price, top performing sectors this week, any major |
| 2026-04-30 22:29 UTC | sonar | April 30 2026 stock market: Give me 4 specific stock tickers with strong catalysts in the last 48 hours. Focus on: earnings beats this week, breakouts above resistance, sector leaders in Technology, E |
| 2026-05-01 17:32 CDT | sonar | List all scheduled US economic events for the next 14 calendar days starting 2026-05-01. For each event return: date (YYYY-MM-DD), time (Eastern, HH:MM 24h), event name (e.g. CPI YoY, FOMC Minutes, In |
| 2026-05-01 17:32 CDT | sonar | List all scheduled US economic events for the next 14 calendar days starting 2026-05-01. For each event return: date (YYYY-MM-DD), time (Eastern, HH:MM 24h), event name (e.g. CPI YoY, FOMC Minutes, In |
| 2026-05-01 17:33 CDT | sonar | List all scheduled US economic events for the next 14 calendar days starting 2026-05-01. For each event return: date (YYYY-MM-DD), time (Eastern, HH:MM 24h), event name (e.g. CPI YoY, FOMC Minutes, In |
| 2026-05-01 17:33 CDT | sonar | List all scheduled US economic events for the next 14 calendar days starting 2026-05-01. For each event return: date (YYYY-MM-DD), time (Eastern, HH:MM 24h), event name (e.g. CPI YoY, FOMC Minutes, In |
| 2026-05-01 18:24 CDT | sonar | List every S&P 500 company and US mega-cap (market cap > $50B) reporting earnings in the next 30 calendar days starting 2026-05-01. For each, return: symbol (ticker, uppercase), company (short name),  |
| 2026-05-01 18:24 CDT | sonar | List every S&P 500 company and US mega-cap reporting earnings in the next 30 calendar days starting 2026-05-01. For each, return: symbol (ticker, uppercase), company (short name), date (YYYY-MM-DD), t |
| 2026-05-01 18:25 CDT | sonar | List every S&P 500 company and US mega-cap (market cap > $50B) reporting earnings in the next 30 calendar days starting 2026-05-01. For each, return: symbol (ticker, uppercase), company (short name),  |
| 2026-05-01 18:25 CDT | sonar | What S&P 500 and mega-cap companies are reporting earnings in May 2026? List the symbol, date (YYYY-MM-DD), and time (BMO or AMC) for each. |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for NVDA (NVIDIA)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for AAPL (Apple)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for MSFT (Microsoft)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for AMZN (Amazon)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for GOOGL (Alphabet)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for META (Meta Platforms)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": " |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for TSLA (Tesla)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for JPM (JPMorgan Chase)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "B |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for BAC (Bank of America)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": " |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for WFC (Wells Fargo)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for GS (Goldman Sachs)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for MS (Morgan Stanley)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BM |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for CRM (Salesforce)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for ORCL (Oracle)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for ADBE (Adobe)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for NFLX (Netflix)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (b |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for AMD (Advanced Micro Devices)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "t |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for AVGO (Broadcom)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" ( |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for WMT (Walmart)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for COST (Costco)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for HD (Home Depot)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" ( |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for TGT (Target)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for NKE (Nike)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (befor |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for MCD (McDonald's)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for SBUX (Starbucks)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for XOM (ExxonMobil)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for CVX (Chevron)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for JNJ (Johnson & Johnson)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for UNH (UnitedHealth)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for LLY (Eli Lilly)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" ( |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for PFE (Pfizer)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for ABBV (AbbVie)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for BRK.B (Berkshire Hathaway)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "typ |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for V (Visa)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (before  |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for MA (Mastercard)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" ( |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for DIS (Disney)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for BA (Boeing)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (befo |
| 2026-05-01 18:27 CDT | sonar | When is the next earnings report for CAT (Caterpillar)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for NVDA (NVIDIA)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for AAPL (Apple)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for MSFT (Microsoft)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for AMZN (Amazon)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for GOOGL (Alphabet)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for META (Meta Platforms)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": " |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for TSLA (Tesla)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for JPM (JPMorgan Chase)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "B |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for BAC (Bank of America)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": " |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for WFC (Wells Fargo)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for GS (Goldman Sachs)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for MS (Morgan Stanley)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BM |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for CRM (Salesforce)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for ORCL (Oracle)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for ADBE (Adobe)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for NFLX (Netflix)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (b |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for AMD (Advanced Micro Devices)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "t |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for AVGO (Broadcom)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" ( |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for WMT (Walmart)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for COST (Costco)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for HD (Home Depot)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" ( |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for TGT (Target)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for NKE (Nike)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (befor |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for MCD (McDonald's)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for SBUX (Starbucks)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for XOM (ExxonMobil)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO"  |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for CVX (Chevron)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for JNJ (Johnson & Johnson)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for UNH (UnitedHealth)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for LLY (Eli Lilly)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" ( |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for PFE (Pfizer)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for ABBV (AbbVie)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (be |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for BRK.B (Berkshire Hathaway)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "typ |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for V (Visa)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (before  |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for MA (Mastercard)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" ( |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for DIS (Disney)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (bef |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for BA (Boeing)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" (befo |
| 2026-05-01 18:28 CDT | sonar | When is the next earnings report for CAT (Caterpillar)? Return ONLY a JSON object — no prose, no citations — with these fields: {"date": "YYYY-MM-DD" or "" if unknown or > 90 days away,  "type": "BMO" |
